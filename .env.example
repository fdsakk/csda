# HTTP Basic Auth for the web UI (browser login prompt).
# Set both variables to enable authentication; leave them unset to disable.
# Everything except /api/health (used by container health checks) requires login.
CSDA_AUTH_USER=admin
CSDA_AUTH_PASSWORD=change-me
